{
  "name": "procrastinators-functions",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p .",
    "lint": "eslint .",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "start": "npm run build && npm run serve"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.0.1",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "eslint": "^9.9.0",
    "typescript": "^5.4.5"
  }
}

